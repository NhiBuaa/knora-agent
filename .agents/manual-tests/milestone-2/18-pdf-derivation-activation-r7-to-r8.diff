diff --git "a/.agents\\manual-tests\\milestone-2\\18-pdf-derivation-activation-r7.md" "b/.agents\\manual-tests\\milestone-2\\18-pdf-derivation-activation-r8.md"
index 46d07d5..7424e07 100644
--- "a/.agents\\manual-tests\\milestone-2\\18-pdf-derivation-activation-r7.md"
+++ "b/.agents\\manual-tests\\milestone-2\\18-pdf-derivation-activation-r8.md"
@@ -2,18 +2,18 @@
 
 ## Metadata
 
-- Status: Draft — synchronization revision awaiting final human review. Do not implement or execute
-  this guide.
+- Status: Final approved and immutable
 - Feature: Milestone 2 — Production-shaped ingestion
 - Slice: GitHub issue #18 — PDF derivation, embedding, and CAS activation
 - Authoritative specification: https://github.com/NhiBuaa/knora-agent/issues/18
 - Design: `docs/design/issue-18-pdf-derivation-activation.md`
-- Guide revision: `m2-issue-18-r7`
-- Supersedes: locked `m2-issue-18-r6` at
-  `.agents/manual-tests/milestone-2/18-pdf-derivation-activation-r6.md`; r1 through r6 remain
+- Guide revision: `m2-issue-18-r8`
+- Supersedes: draft `m2-issue-18-r7` at
+  `.agents/manual-tests/milestone-2/18-pdf-derivation-activation-r7.md`; r1 through r7 remain
   unchanged.
-- Approved by: Pending
-- Approved at: Pending
+- Approved by: Nhi (explicit human approval in Codex task)
+- Approved at: 2026-08-09T22:37:58+07:00
+- Manual-acceptance state: Approved for implementation; execution remains pending implementation.
 
 ## Authority and fixed test conventions
 
@@ -59,7 +59,7 @@ not invent a synchronous cleanup action that no approved Issue #18 contract defi
 | Original-object checksum and ObjectStore metadata are verified; reads stream | TC-07A1–TC-07A5; TC-07B steps 1–3 | Each required claimed-vs-observed metadata component (Workspace, opaque key, raw SHA-256, byte size, media type) is independently rejected before open/read/extraction/provider; TC-07B separately proves no whole-object read or complete logical parent retention | Per-field injected mismatch/category, zero open/extractor/provider counts, zero derivation/activation delta, unchanged active pointer; parent raw-source-retention/peak-live-byte trace |
 | Parser/chunker creates or reuses the correct PDF Chunk Set | TC-01A steps 1–3; TC-05A steps 1–3 | Exact full derivation identity creates/reuses one immutable complete Chunk Set | Chunk Set identity, chunk ordinal/checksum/page-offset projection |
 | Embedding calls occur outside DB transactions and validate count/dimension/provider/model/configuration | TC-01A step 2; TC-01C steps 1–3; TC-03B steps 1–3 | No handler/configuration/persistence or finalization/activation transaction encloses remote work; independent short heartbeat transactions may overlap | Correlated transaction ownership trace, finalization lock trace, provider trace, vector validation projection |
-| Retry taxonomy covers the Issue #18 PDF-handler categories; deterministic failures are terminal | TC-03A–TC-03E and TC-03G–TC-03L; TC-03F is not applicable | Every executable fixed injection maps to its named failure kind/cause and policy disposition; coordinator PostgreSQL failures follow Issue #17 reconciliation/indeterminate semantics | Handler fact, canonical cause, attempt history, policy metadata, call counts and coordination reconciliation evidence |
+| Retry taxonomy covers the Issue #18 PDF-handler categories; deterministic failures are terminal | Executable handler taxonomy: TC-03A–TC-03E and TC-03G–TC-03L. `DATABASE_TRANSIENT`: TC-03F is not applicable. Coordinator semantics: TC-05B, TC-05C and TC-06C. | Handler facts map through the executable TC-03 cases; coordinator PostgreSQL failures retain Issue #17 reconciliation/indeterminate semantics rather than entering handler taxonomy. | Handler cause/policy/attempt evidence for TC-03; same-operation reconciliation evidence from TC-05B; definite pre-commit persistence-failure evidence from TC-05C; lease-expiry recovery evidence from TC-06C. |
 | Final persistence atomically creates/reuses a complete chain with no partial retrieval-visible derivation | TC-01A; TC-05C steps 1–3 | Commit has complete chain plus terminal outcome, while definite rollback has neither | Transaction outcome, all derivation row counts, active pointer, attempt/job fields |
 | Activation CAS requires live fencing, current target and same Document/Workspace complete set | TC-01A; TC-04; TC-06B; TC-08A–TC-08C | Valid claim activates; stale claim is fenced; stale target is superseded; invalid pointers are rejected | Token/result, pointer state, transaction result and DB constraint error class |
 | Newer source makes old target superseded without another retry and records replacement when available | TC-04 steps 1–4 | Older job reaches `superseded`, not failed/retried, while newer version stays current/served | A/B job snapshots, attempt count, replacement fields, current/active/served projection |
@@ -193,10 +193,11 @@ not invent a synchronous cleanup action that no approved Issue #18 contract defi
 
 ### TC-03: Classify deterministic worker failures and retry only approved transient facts
 
-Every subcase starts from attempt 1 of one profile-A job. For `ScheduleRetry`, expected policy is
-V1 `[0, 5 seconds]` full jitter recorded once; a separately controlled run advances to the exact
-persisted `next_attempt_at` and proves claim of attempt 2. For `FailTerminal`, expected attempt
-count remains one and there is no provider/extractor call after the named failure point.
+Every executable handler subcase starts from attempt 1 of one profile-A job. For `ScheduleRetry`,
+expected policy is V1 `[0, 5 seconds]` full jitter recorded once; a separately controlled run
+advances to the exact persisted `next_attempt_at` and proves claim of attempt 2. For
+`FailTerminal`, expected attempt count remains one and there is no provider/extractor call after the
+named failure point.
 
 | Subcase | Fixed injection | Expected handler kind -> canonical cause | Expected disposition / safe public result | Required call count and persistence delta |
 | --- | --- | --- | --- | --- |
@@ -221,9 +222,15 @@ persistence/reconciliation or indeterminate-infrastructure semantics, never
 allowlisted public codes are valid for transient categories unless an approved contract names a
 variant-specific code.
 
-Evidence for every subcase is the injected sentinel, handler kind, canonical cause, policy result,
-safe code, full attempt-history retry/terminal fields, call counts, and zero derivation/active
-pointer delta. A definitive coordination persistence failure is not entered in this matrix.
+Evidence for every executable handler subcase is the injected sentinel, handler kind, canonical
+cause, policy result, safe code, full attempt-history retry/terminal fields, call counts, and zero
+derivation/active-pointer delta.
+
+TC-03F evidence is the authoritative Issue #18 criterion, the Issue #17 cause/persistence
+contract, proof that the PDF handler has no handler-owned database dependency, and absence of a
+handler injection. Coordination failure/reconciliation evidence comes from TC-05B's same-operation
+read-back, TC-05C's definite pre-commit persistence failure and TC-06C's `LEASE_EXPIRED` recovery;
+it is never an invented handler outcome.
 
 ### TC-04: Supersede an older target with CAS while newer knowledge stays served
 
@@ -451,5 +458,5 @@ retain their required observable behavior without preselecting those mechanisms.
 
 This guide becomes immutable only after explicit human approval. Any resolution of an ambiguity,
 semantic test change, or authoritative-specification change requires a new revision; do not rewrite
-`m2-issue-18-r7` after approval. Store future execution observations separately as JSONL
+`m2-issue-18-r8` after approval. Store future execution observations separately as JSONL
 Evaluation records.

{
  "schema_version": 1,
  "base_packet_reference": ".agents/review/m4-issue-77-ticket-review-packet-v6.json",
  "base_packet_digest": "sha256:fc292332c86d0ea2426a0ada78afb990584a63643be5c96c9ab657a45b503ded",
  "payload": {
    "delta": {
      "guide_revision": "m4-77-ticket-contract-v9",
      "guide_digest": "sha256:bce76ae5dd7c7a7668c7675c8df2e3251dbfe41424ac7687ea3e83fd1fa875b5",
      "ticket_digest": "sha256:bce76ae5dd7c7a7668c7675c8df2e3251dbfe41424ac7687ea3e83fd1fa875b5",
      "acceptance_criteria": [
        {
          "id": "M4-77-AC-01",
          "statement": "WriteProposalWorkflow.handle remains the sole application-facing write Interface. Every pre-acquisition Workspace/resource/execution authorization, exact capability/binding/policy compatibility, complete m4r1 validity or proposal-expiry denial creates no execution/acquisition/admission/audit-start artifact and zero provider calls. Read/observation authority cannot escalate. Temporary execution-authority denial preserves exact approved state and approval identity/version/digest. Only material capability, external-scope binding or policy incompatibility persists stale/invalidated with one approved closed CompatibilityCheckerV1 reason; immutable target/reference replacement is rejected and requires a new proposal plus authorized-human approval. Reference syntax, integrity, scope, trusted-store, key and expiry denial fails closed while the exact approved proposal and approval provenance remain unchanged and invents no reference CompatibilityReason."
        },
        {
          "id": "M4-77-AC-02",
          "statement": "The logical execution ID and canonical-json-v1 complete-intent fingerprint are server-owned, immutable and absent from writable application/HTTP command schemas; attempted injection is rejected before state creation. An independent oracle recomputes the exact fingerprint from authoritative server-normalized operation, capability, scope, protected reference/resource and title/description inputs and compares both identities byte-for-byte at proposal, acquisition, admission, persisted envelope and provider verification boundaries. acquire_execution uses fresh post-lock PostgreSQL time and atomically commits one complete generation-1 AcquireWitness; every identified loser makes zero gateway/provider calls and every pre-commit fault leaves no partial tuple or caller-influenced identity."
        },
        {
          "id": "M4-77-AC-03",
          "statement": "A fixed global reference-key plus Workspace dispatch epoch vector serializes all supported authority/selector/key mutation adapters. After acquisition, authorize_and_admit_dispatch locks execution and both epochs, reloads all current facts and fully reverifies exact m4r1 canonical bytes, MAC, protected claims/scope, trusted-store equality, key identity/status and expiry. An independent matrix exercises non-canonical bytes, MAC corruption, protected-claim corruption, protected-scope corruption, trusted-store delete/replacement/claims mismatch, revoked/unknown/unavailable key and reference expiry. Non-canonical input maps to 400/INVALID_TOOL_RESOURCE_REFERENCE; authenticated integrity/scope/store/key/expiry denial maps to 403/TOOL_RESOURCE_ACCESS_DENIED. Every row binds durable identities, persists no admission/admission-audit and makes zero gateway/provider calls."
        },
        {
          "id": "M4-77-AC-04",
          "statement": "Fresh post-lock PostgreSQL time and current owner/generation plus unexpired lease/reference atomically commit at most one immutable DispatchAdmissionWitness per logical execution. One private row binds every schema/purpose, Workspace/proposal/logical identity/fingerprint, exact capability/binding/policy/reference and complete-intent digest, authority decision/epoch witness, owner/generation, database times, non-secret signing provenance, opaque routing digest, exact envelope bytes/digest and linked audit field. A deterministic commit/ack-ambiguity fixture captures the exact intended tuple/bytes: authoritative readback returns that one byte-identical complete tuple with no second insert/sign/regeneration if committed, or complete admission/audit absence with no gateway call or #77 retry if rolled back; no partial or third state is valid."
        },
        {
          "id": "M4-77-AC-05",
          "statement": "Admission commit is the logical start of the provider write. The exact privately persisted m4-dispatch-admission-v1 envelope is the only create_ticket input. After admission-first, later authority/selector/key mutation or proposal/reference/lease expiry cannot reauthorize, cancel, replace, upgrade or retarget the dispatch; the original byte-identical envelope/intent is sent exactly once. Mutation-first remains denied."
        },
        {
          "id": "M4-77-AC-06",
          "statement": "The SQLite reference-provider Adapter verifies envelope integrity and recomputed signed-intent fingerprint before its provider-owned logical-ID ledger. One BEGIN IMMEDIATE commits effect or closed rejection with outcome. Same ID/fingerprint replays; two independently valid test-harness envelopes with the same ID and different self-consistent fingerprints reach ProviderIdempotencyConflict before target lookup/mutation and create no second effect; tampering fails earlier. Separately, each of target_not_found, validation_rejected and policy_rejected atomically persists exactly one ledger outcome with exact logical ID/fingerprint/admission digest, creates zero ticket/target effects, replays identically and survives provider restart; every pre-commit fault leaves neither row nor effect."
        },
        {
          "id": "M4-77-AC-07",
          "statement": "Every post-admission crash/session/transport/ack uncertainty remains executing with the same outstanding admission/logical identity/fingerprint and typed indeterminate_external_outcome until Knora finalizes. Admission-before-gateway and pre-SQLite-commit branches have exactly zero provider ledger rows/effects; committed-before-ack has exactly one durable replayable success effect/outcome or one named closed-rejection outcome with zero effects. A read-only provider lookup before receipt yields provider_outcome_not_found without retiring/finalizing admission or granting retry. #77 reloads NoAdmission or AdmissionOutstanding recovery seeds but performs no observation workflow, retry, replay, takeover, first-admission recovery or generation 2+."
        },
        {
          "id": "M4-77-AC-08",
          "statement": "record_execution_observation and finalize_execution use fresh post-lock PostgreSQL time and require current generation-1 owner and lease. For delayed success and each closed rejection, fixtures start with the SQLite outcome committed, no PostgreSQL observation and lifecycle executing. After expiry, each operation is fenced and a before/after PostgreSQL snapshot remains identical: no observation row, no observation/audit append and no terminal lifecycle/outcome rewrite; the authoritative SQLite outcome remains readable after provider restart."
        },
        {
          "id": "M4-77-AC-09",
          "statement": "Only direct success and target_not_found, validation_rejected or policy_rejected finalize; receipt-possible results remain executing/202 and provider fingerprint conflict remains executing/409 with no retry authority. Every success, closed-rejection, indeterminate, conflict, fenced and post-acquisition reference-denial application/HTTP projection matches a closed discriminated allowlist exactly and recursively excludes raw provider/routing, admission/envelope/MAC/fingerprint, authority epoch/decision/witness, key/signing, credential/secret and every other private field. Protected-scope corruption and reference expiry remain generation-1 executing with unchanged identities, no admission or terminal outcome after restart. Observation/read authority never constructs write authority or an admission envelope."
        },
        {
          "id": "M4-77-AC-10",
          "statement": "A post-restart canonical append-only audit projection exactly reconstructs caller/proposal/approval/execution actors, both current decisions, compatibility, complete acquisition/admission witnesses, authority epochs, database times, provider alias, immutable intent/identity/fingerprint, observations and outcome while excluding raw provider IDs/routing, envelope bytes/MAC, keys, credentials and authority secrets. M1-M3 regressions remain green."
        }
      ],
      "test_cases": [
        {
          "id": "M4-77-TC-01",
          "statement": "At application and HTTP seams, independently deny every pre-acquisition class and Execute with principals proven to have only read/observation authority; capture proposal and approval before/after every denial. Separately force material capability, binding and policy incompatibility, every reference syntax/integrity/scope/trusted-store/key/expiry denial, and attempted material target/reference substitution.",
          "oracle": "Every branch has no execution/acquisition/admission/audit-start artifact and zero provider calls. Temporary execution-authority denial preserves exact approved state and approval identity/version/digest. Capability/binding/policy mismatch persists exact stale/invalidated state with an approved closed reason and permanently rejects old approval reuse. Every reference denial preserves the exact immutable approved proposal and approval provenance; material substitution is rejected, a usable new target requires a new proposal plus human approval, and no reference CompatibilityReason is invented.",
          "required": true
        },
        {
          "id": "M4-77-TC-02",
          "statement": "At application/HTTP seams prove writable command schemas omit logical execution ID and request fingerprint and reject injection before state creation. Independently recompute canonical-json-v1 from authoritative normalized intent and compare both server identities across proposal, acquisition, admission, persisted envelope and provider evidence; then race identified public-workflow contenders and inject faults after each acquisition write before commit.",
          "oracle": "The recomputed fingerprint and server-minted logical ID are byte-identical and immutable at every boundary and cannot be caller-influenced. One AcquireApplied contender owns one complete atomic tuple and is the only possible gateway caller; every loser has per-contender zero calls. Separate snapshots see no pre-commit partial state; rollback restores approved with no tuple or caller-influenced identity; restart reconstructs every acquisition field.",
          "required": true
        },
        {
          "id": "M4-77-TC-03",
          "statement": "After acquisition and while admission is blocked, independently exercise every real authority/selector mutation and distinct m4r1 rows for non-canonical bytes, MAC corruption, protected-claim corruption, protected-scope corruption, trusted-store delete/replacement/claims mismatch, revoked/unknown/unavailable key and reference expiry using non-production mutation adapters.",
          "oracle": "Every row compares the exact private ProposalNotExecutable reason, application projection and decoded HTTP envelope: non-canonical uses invalid_tool_resource_reference plus 400/INVALID_TOOL_RESOURCE_REFERENCE; authenticated integrity/scope/store/key/expiry uses resource_access_denied plus 403/TOOL_RESOURCE_ACCESS_DENIED. Independently reloaded durable proposal/logical-execution identities match; no admission/admission-audit row exists and gateway/provider calls remain zero.",
          "required": true
        },
        {
          "id": "M4-77-TC-04",
          "statement": "Race every real typed mutation Adapter against admission, race same-generation issuers, and block acquisition across proposal expiry plus admission across reference/lease expiry under non-database clock skew.",
          "oracle": "Mutation-first denies or admission-first binds the exact prior vector; one byte-identical admission exists. Fresh post-lock PostgreSQL time decides each expiry and a transaction-start clock cannot revive it. Admission-side reference expiry returns the exact TC-03 resource_access_denied and 403/TOOL_RESOURCE_ACCESS_DENIED result with no admission/audit/provider activity.",
          "required": true
        },
        {
          "id": "M4-77-TC-05",
          "statement": "Read the committed admission row/audit in one snapshot, compare every AC-04 field including the independently recomputed server-owned fingerprint and immutable logical ID to authoritative inputs, restart PostgreSQL and rotate the active issuance key. At the commit/ack fault seam capture the exact intended complete tuple/bytes and force committed-but-unacknowledged and rolled-back outcomes.",
          "oracle": "Committed readback returns exactly the intended tuple, identities and persisted bytes with no second insert/sign/regeneration. Rolled-back readback returns complete admission/audit absence with no gateway call or #77 retry. No partial, caller-influenced or third state is valid.",
          "required": true
        },
        {
          "id": "M4-77-TC-06",
          "statement": "Pause immediately after admission commit; independently mutate/revoke every authority/selector/key class and cross proposal/reference/lease expiry before resuming.",
          "oracle": "Exactly one gateway call carries the original byte-identical envelope and intent with no reauthorization, cancellation, replacement, upgrade or retarget; later lease expiry may fence only Knora finalization.",
          "required": true
        },
        {
          "id": "M4-77-TC-07",
          "statement": "In private provider contract composition, deliver same-ID/same-fingerprint replay, two independently valid self-consistent same-ID/different-fingerprint envelopes, separate tampered/cross-bound envelopes, and parameterized target_not_found, validation_rejected and policy_rejected requests with pre-commit faults.",
          "oracle": "Equal fingerprint replays one effect; both valid unequal fingerprints pass integrity and provider recomputation from signed authoritative intent, the normal envelope equals TC-02's independent server recomputation, and the second reaches provider ledger conflict before target lookup with no second effect; tampering fails before SQLite. Each closed rejection commits one exact logical-ID/fingerprint/admission-digest ledger outcome, creates zero target effects, replays identically and survives provider restart; its pre-commit fault leaves neither row nor effect.",
          "required": true
        },
        {
          "id": "M4-77-TC-08",
          "statement": "Fault after admission before gateway, before SQLite commit and after SQLite commit before acknowledgement as three named branches; call only the read-only provider observer in no-receipt branches and independently cover success plus all three closed rejections in committed-before-ack.",
          "oracle": "The first two branches have exactly zero provider ledger rows and zero ticket/target effects. Committed-before-ack has exactly one durable replayable ledger outcome and exactly one success effect or one named closed rejection with zero effects, unchanged after provider restart. Every not-yet-finalized branch remains executing with the same admission/logical ID/fingerprint and exact indeterminate_external_outcome and grants no #77 retry/replay/takeover; no-receipt lookup returns provider_outcome_not_found.",
          "required": true
        },
        {
          "id": "M4-77-TC-09",
          "statement": "After restart reload executing with no admission and executing with outstanding admission as the two exact ExecutionRecoverySeed variants, then call Execute again.",
          "oracle": "Execute returns current ExecutionInProgress; #77 performs no provider observation, admission creation/replacement, dispatch/replay, retry authorization, takeover or generation increment in either branch.",
          "required": true
        },
        {
          "id": "M4-77-TC-10",
          "statement": "Separately delay success, target_not_found, validation_rejected and policy_rejected after SQLite commit but before PostgreSQL observation. Cross lease expiry, invoke observation/finalization under deterministic lock barriers, restart/read provider, and cover every success, closed-rejection, indeterminate, conflict, fenced and TC-03 post-acquisition denial application/HTTP projection.",
          "oracle": "Each expired operation uses fresh post-lock PostgreSQL time, returns ExecutionFenced and leaves before/after PostgreSQL state identical; provider truth survives restart. Every result equals the exact discriminated allowlist with no extra recursive key. Protected-scope corruption and reference expiry link to TC-03/TC-04 evidence and, after PostgreSQL restart, remain generation-1 executing with unchanged proposal/logical ID/fingerprint/acquisition, no admission, no terminal outcome/finalization row, no terminal-audit append and zero provider calls.",
          "required": true
        },
        {
          "id": "M4-77-TC-11",
          "statement": "Restart PostgreSQL and SQLite independently after direct, indeterminate, conflict and no-admission outcomes and reconstruct audit solely from append-only records.",
          "oracle": "The full AC-10 projection matches proposal/acquisition/admission/provider evidence; provider ledger remains independent and every excluded raw/secret field is absent.",
          "required": true
        },
        {
          "id": "M4-77-TC-12",
          "statement": "Run focused M4.3, full repository pytest, Ruff, Compose and clean Alembic verification on the exact candidate SHA.",
          "oracle": "All candidate checks and M1-M3 regressions are green.",
          "required": true
        }
      ],
      "required_evidence": [
        {
          "id": "M4-77-EV-01",
          "statement": "Rejected caller identity injection, independent canonical fingerprint recomputation, byte-identical server-owned logical ID/fingerprint across proposal/acquisition/admission/envelope/provider, per-contender acquisition results/call counts, pre-commit isolation, rollback absence and exact post-commit/restart AcquireWitness tuple.",
          "required": true
        },
        {
          "id": "M4-77-EV-02",
          "statement": "Real mutation-adapter epoch traces, the complete post-acquisition reference-denial matrix including protected-scope corruption and reference expiry, exact private/application/HTTP mappings, independently reloaded durable identities, zero admission/audit/provider activity, and fresh post-lock PostgreSQL expiry samples.",
          "required": true
        },
        {
          "id": "M4-77-EV-03",
          "statement": "One complete admission-row witness, linked audit, exact persisted envelope bytes, key-rotation reload, admission-first non-cancellation traces, and both authoritative branches of the commit/ack ambiguity fixture with no second insert/sign/regeneration or partial state.",
          "required": true
        },
        {
          "id": "M4-77-EV-04",
          "statement": "Provider-owned named evidence for target_not_found, validation_rejected and policy_rejected, each binding the exact logical execution ID/fingerprint/admission digest, one closed SQLite ledger outcome, zero ticket/target effects, pre-commit rollback absence, equal-fingerprint replay and the identical authoritative outcome after provider restart; plus the two-valid-envelope conflict and tamper rejection.",
          "required": true
        },
        {
          "id": "M4-77-EV-05",
          "statement": "Three branch-specific post-admission provider/Knora state records proving exact zero/zero before receipt, exact one durable outcome after commit, unchanged executing/admission/logical ID/fingerprint plus indeterminate_external_outcome before Knora finalization, provider-outcome-not-found versus closed-rejection persistence, two-variant recovery seed and absence of all #78 behavior.",
          "required": true
        },
        {
          "id": "M4-77-EV-06",
          "statement": "Temporary-authority/reference-denial approved-provenance versus capability/binding/policy stale/invalidated snapshots, immutable target-substitution rejection, exact public discriminated allowlist and forbidden-field matrices for every result, protected-scope/reference-expiry post-restart non-finalization evidence, complete expiry-fenced PostgreSQL snapshots, full audit reconstruction, exact subject SHA and all governed verification totals/exits.",
          "required": true
        }
      ],
      "prior_findings": [
        {
          "category": "security",
          "issue_class": "material_reference_mismatch_stale_invalidation_not_exercised",
          "criterion_ref": "M4-77-AC-01",
          "affected_case_ids": [
            "M4-77-TC-01"
          ],
          "affected_fields": [
            "compatibility_reason",
            "proposal_state",
            "approval_validity",
            "immutable_reference"
          ],
          "problem": "Guide v6 exposed a ticket-v8 conflict by requiring material reference mismatch to use stale/invalidation even though the authoritative closed CompatibilityCheckerV1 taxonomy covers only capability, scope-binding and policy mismatches.",
          "required_change": "Restore the approved closed taxonomy, reject immutable target/reference replacement, keep reference validity denial fail-closed with unchanged approval provenance, and require a new proposal plus approval for a different target without inventing a reference CompatibilityReason.",
          "status": "ADDRESSED_IN_V9"
        },
        {
          "category": "coverage",
          "issue_class": "post_acquisition_reference_denial_matrix_incomplete",
          "criterion_ref": "M4-77-AC-03",
          "affected_case_ids": [
            "M4-77-TC-03",
            "M4-77-TC-04",
            "M4-77-TC-10"
          ],
          "affected_fields": [
            "protected_scope",
            "reference_expiry",
            "application_projection",
            "http_envelope",
            "restart_state"
          ],
          "problem": "Guide v6 omitted protected-scope corruption and reference-expiry rows from the post-acquisition denial matrix and therefore lacked complete application/HTTP, zero-activity and restart-stable non-finalization oracles.",
          "required_change": "Add both rows with exact typed denial mappings, independently sourced durable identities, fresh PostgreSQL-time linkage, zero admission/audit/gateway/provider activity and post-restart generation-1 non-finalization evidence.",
          "status": "ADDRESSED_IN_V9"
        }
      ]
    }
  },
  "packet_digest": "sha256:097514f4b155d43aa40293d827a397e66962523761423c02d311bfc8f564476a",
  "request_id": "review-request-sha256:bdc313483f34566d012d86082d4edba1f214963e259f6ca068ae3fab6be178fa"
}

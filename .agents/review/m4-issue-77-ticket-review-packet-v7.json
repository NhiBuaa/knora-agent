{
  "schema_version": 1,
  "base_packet_reference": ".agents/review/m4-issue-77-ticket-review-packet-v6.json",
  "base_packet_digest": "sha256:fc292332c86d0ea2426a0ada78afb990584a63643be5c96c9ab657a45b503ded",
  "payload": {
    "delta": {
      "guide_revision": "m4-77-ticket-contract-v7",
      "guide_digest": "sha256:f0c5cf68b4cfec0889d7c5127817293c12bb61b0f53781dcf8ef4d7b219ee922",
      "ticket_digest": "sha256:f0c5cf68b4cfec0889d7c5127817293c12bb61b0f53781dcf8ef4d7b219ee922",
      "acceptance_criteria": [
        {"id": "M4-77-AC-01", "statement": "WriteProposalWorkflow.handle remains the sole application-facing write Interface. Every pre-acquisition Workspace/resource/execution authorization, exact compatibility, complete m4r1 or proposal-expiry denial creates no execution/acquisition/admission/audit-start artifact and zero provider calls. Independently granted read-only or observation-only authority cannot escalate to execution/write authority."},
        {"id": "M4-77-AC-02", "statement": "The server-owned logical ID and canonical-json-v1 complete-intent fingerprint remain immutable. acquire_execution uses fresh post-lock PostgreSQL time and atomically commits one complete generation-1 AcquireWitness containing executing lifecycle/revision, owner/generation, database lease, authorized binding snapshot, proposal/logical ID/fingerprint and linked acquisition audit. Every identified concurrent loser makes zero gateway/provider calls and every pre-commit fault leaves no partial tuple."},
        {"id": "M4-77-AC-03", "statement": "A fixed global reference-key plus Workspace dispatch epoch vector serializes all supported authority/selector/key mutation adapters. After acquisition, authorize_and_admit_dispatch locks execution and both epochs, reloads all current facts and fully reverifies exact m4r1 canonical bytes, MAC, claims, trusted-store equality, key identity/status and expiry. Independent post-acquisition corruption/trusted-store/key/authority mutations deny with no admission or provider call."},
        {"id": "M4-77-AC-04", "statement": "Fresh post-lock PostgreSQL time and current owner/generation plus unexpired lease/reference atomically commit at most one immutable DispatchAdmissionWitness per logical execution. One private row binds every schema/purpose, Workspace/proposal/logical identity/fingerprint, exact capability/binding/policy/reference and complete-intent digest, authority decision/epoch witness, owner/generation, database times, non-secret signing provenance, opaque routing digest, exact envelope bytes/digest and linked audit field. A deterministic commit/ack-ambiguity fixture captures the exact intended tuple/bytes: authoritative readback returns that one byte-identical complete tuple with no second insert/sign/regeneration if committed, or complete admission/audit absence with no gateway call or #77 retry if rolled back; no partial or third state is valid."},
        {"id": "M4-77-AC-05", "statement": "Admission commit is the logical start of the provider write. The exact privately persisted m4-dispatch-admission-v1 envelope is the only create_ticket input. After admission-first, later authority/selector/key mutation or proposal/reference/lease expiry cannot reauthorize, cancel, replace, upgrade or retarget the dispatch; the original byte-identical envelope/intent is sent exactly once. Mutation-first remains denied."},
        {"id": "M4-77-AC-06", "statement": "The SQLite reference-provider Adapter verifies envelope integrity and recomputed signed-intent fingerprint before its provider-owned logical-ID ledger. One BEGIN IMMEDIATE commits effect or closed rejection with outcome. Same ID/fingerprint replays; two independently valid test-harness envelopes with the same ID and different self-consistent fingerprints reach ProviderIdempotencyConflict before target lookup/mutation and create no second effect; tampering fails earlier. Separately, each of target_not_found, validation_rejected and policy_rejected atomically persists exactly one ledger outcome with exact logical ID/fingerprint/admission digest, creates zero ticket/target effects, replays identically and survives provider restart; every pre-commit fault leaves neither row nor effect."},
        {"id": "M4-77-AC-07", "statement": "Every post-admission crash/session/transport/ack uncertainty remains executing with the same outstanding admission/logical identity/fingerprint and typed indeterminate_external_outcome. A read-only provider contract-harness lookup before receipt yields provider_outcome_not_found and does not retire/finalize the admission or grant retry; direct target_not_found, validation_rejected and policy_rejected are distinct ledgered terminal business rejections. #77 reloads NoAdmission or AdmissionOutstanding recovery seeds but performs no observation workflow, retry, replay, takeover, first-admission recovery or generation 2+."},
        {"id": "M4-77-AC-08", "statement": "record_execution_observation and finalize_execution use fresh post-lock PostgreSQL time and require current generation-1 owner and lease. For delayed success and each closed rejection, fixtures start with the SQLite outcome committed, no PostgreSQL observation and lifecycle executing. After expiry, each operation is fenced and a before/after PostgreSQL snapshot remains identical: no observation row, no observation/audit append and no terminal lifecycle/outcome rewrite; the authoritative SQLite outcome remains readable after provider restart."},
        {"id": "M4-77-AC-09", "statement": "Only direct success and target_not_found, validation_rejected or policy_rejected finalize; receipt-possible results remain executing/202 and provider fingerprint conflict remains executing/409 with no retry authority. Public projections are sanitized and observation/read authority never constructs write authority or an admission envelope."},
        {"id": "M4-77-AC-10", "statement": "A post-restart canonical append-only audit projection exactly reconstructs caller/proposal/approval/execution actors, both current decisions, compatibility, complete acquisition/admission witnesses, authority epochs, database times, provider alias, immutable intent/identity/fingerprint, observations and outcome while excluding raw provider IDs/routing, envelope bytes/MAC, keys, credentials and authority secrets. M1-M3 regressions remain green."}
      ],
      "test_cases": [
        {"id": "M4-77-TC-01", "statement": "At application and HTTP seams, independently deny every pre-acquisition class and Execute with principals proven to have only read, only observation, or both read/observation authority without execution authority.", "oracle": "Each returns the exact denial with no execution/acquisition/admission/audit-start artifact and zero provider calls; presence of the limited grants is independently asserted.", "required": true},
        {"id": "M4-77-TC-02", "statement": "Race identified public-workflow contenders and inject faults after each acquisition write before commit.", "oracle": "One AcquireApplied contender owns one complete atomic tuple and is the only possible gateway caller; every loser has per-contender zero calls. Separate snapshots see no pre-commit partial state; rollback restores approved with no tuple; restart reconstructs every acquisition field.", "required": true},
        {"id": "M4-77-TC-03", "statement": "After acquisition and while admission is blocked, independently exercise every real authority/selector mutation and non-runtime m4r1 canonical-byte/MAC/claims corruption, trusted-store delete/replace/mismatch and key revoke/unknown/rotate/unavailable fixture.", "oracle": "Post-lock reload/reverification observes every class, persists no admission, makes zero gateway/provider calls and appends only sanitized pre-provider evidence.", "required": true},
        {"id": "M4-77-TC-04", "statement": "Race every real typed mutation Adapter against admission, race same-generation issuers, and block acquisition/admission across proposal/reference/lease expiry under non-database clock skew.", "oracle": "Mutation-first denies or admission-first binds the exact prior vector; one byte-identical admission exists. Fresh post-lock PostgreSQL time decides each expiry and a transaction-start clock cannot revive it.", "required": true},
        {"id": "M4-77-TC-05", "statement": "Read the committed admission row/audit in one snapshot, compare every AC-04 field to authoritative inputs, restart PostgreSQL and rotate the active issuance key. At the commit/ack fault seam capture the exact intended complete tuple/bytes and force both committed-but-unacknowledged and rolled-back outcomes.", "oracle": "Committed readback returns exactly the intended tuple and reuses its persisted bytes with no second insert, signing operation or envelope regeneration. Rolled-back readback returns complete admission/audit absence with no gateway call or #77 retry. No partial or third state is valid.", "required": true},
        {"id": "M4-77-TC-06", "statement": "Pause immediately after admission commit; independently mutate/revoke every authority/selector/key class and cross proposal/reference/lease expiry before resuming.", "oracle": "Exactly one gateway call carries the original byte-identical envelope and intent with no reauthorization, cancellation, replacement, upgrade or retarget; later lease expiry may fence only Knora finalization.", "required": true},
        {"id": "M4-77-TC-07", "statement": "In private provider contract composition, deliver same-ID/same-fingerprint replay, two independently valid self-consistent same-ID/different-fingerprint envelopes, separate tampered/cross-bound envelopes, and parameterized target_not_found, validation_rejected and policy_rejected requests with pre-commit faults.", "oracle": "Equal fingerprint replays one effect; both valid unequal fingerprints pass integrity/recomputation and the second reaches provider ledger conflict before target lookup with no second effect; tampering fails before SQLite. Each closed rejection commits one exact logical-ID/fingerprint/admission-digest ledger outcome, creates zero target effects, replays identically and survives provider restart; its pre-commit fault leaves neither row nor effect.", "required": true},
        {"id": "M4-77-TC-08", "statement": "Fault after admission before gateway, before SQLite commit and after SQLite commit before acknowledgement; in the no-receipt window call only the read-only provider contract observer and separately exercise all three direct closed rejections.", "oracle": "Admission survives, provider has zero or one outcome, provider_outcome_not_found leaves the exact admission outstanding/non-terminal with no retry, and target_not_found, validation_rejected and policy_rejected remain three distinct authoritative ledger outcomes after provider restart.", "required": true},
        {"id": "M4-77-TC-09", "statement": "After restart reload executing with no admission and executing with outstanding admission as the two exact ExecutionRecoverySeed variants, then call Execute again.", "oracle": "Execute returns current ExecutionInProgress; #77 performs no provider observation, admission creation/replacement, dispatch/replay, retry authorization, takeover or generation increment in either branch.", "required": true},
        {"id": "M4-77-TC-10", "statement": "Separately delay success, target_not_found, validation_rejected and policy_rejected after SQLite commit but before any PostgreSQL observation. Cross lease expiry, invoke observation and finalization under deterministic lock barriers, then restart and read the provider; also cover every before-expiry and non-terminal application/HTTP mapping.", "oracle": "Each expired operation uses fresh post-lock PostgreSQL time, returns ExecutionFenced and leaves before/after PostgreSQL state identical: no observation row, no observation/audit append and no terminal lifecycle/outcome rewrite. The exact provider outcome remains authoritative after restart; before-expiry and non-terminal mappings match the closed result matrix.", "required": true},
        {"id": "M4-77-TC-11", "statement": "Restart PostgreSQL and SQLite independently after direct, indeterminate, conflict and no-admission outcomes and reconstruct audit solely from append-only records.", "oracle": "The full AC-10 projection matches proposal/acquisition/admission/provider evidence; provider ledger remains independent and every excluded raw/secret field is absent.", "required": true},
        {"id": "M4-77-TC-12", "statement": "Run focused M4.3, full repository pytest, Ruff, Compose and clean Alembic verification on the exact candidate SHA.", "oracle": "All candidate checks and M1-M3 regressions are green.", "required": true}
      ],
      "required_evidence": [
        {"id": "M4-77-EV-01", "statement": "Per-contender acquisition results/call counts, pre-commit isolation, rollback absence and exact post-commit/restart AcquireWitness tuple.", "required": true},
        {"id": "M4-77-EV-02", "statement": "Real mutation-adapter epoch traces, complete post-acquisition m4r1 failure matrix and fresh post-lock PostgreSQL expiry samples.", "required": true},
        {"id": "M4-77-EV-03", "statement": "One complete admission-row witness, linked audit, exact persisted envelope bytes, key-rotation reload, admission-first non-cancellation traces, and both authoritative branches of the commit/ack ambiguity fixture with no second insert/sign/regeneration or partial state.", "required": true},
        {"id": "M4-77-EV-04", "statement": "Provider-owned named evidence for target_not_found, validation_rejected and policy_rejected, each binding the exact logical execution ID/fingerprint/admission digest, one closed SQLite ledger outcome, zero ticket/target effects, pre-commit rollback absence, equal-fingerprint replay and the identical authoritative outcome after provider restart; plus the two-valid-envelope conflict and tamper rejection.", "required": true},
        {"id": "M4-77-EV-05", "statement": "Provider-outcome-not-found versus all three terminal closed-rejection persistence/projection records, provider restart observations, two-variant read-only recovery seed and absence of all #78 behavior in #77.", "required": true},
        {"id": "M4-77-EV-06", "statement": "Sanitized public result matrix, complete post-expiry before/after PostgreSQL snapshots for success and each closed rejection, exact full audit reconstruction, excluded-field proof, exact subject SHA and all governed verification totals/exits.", "required": true}
      ],
      "prior_findings": [
        {"category": "oracle", "issue_class": "dispatch_admission_ambiguous_commit_readback_gap", "criterion_ref": "M4-77-AC-04", "affected_case_ids": ["M4-77-TC-04", "M4-77-TC-05"], "affected_fields": ["DispatchAdmission", "commit_ack_ambiguity", "authoritative_readback", "envelope_bytes", "admission_uniqueness"], "problem": "V6 did not force a commit/ack ambiguity or prove exact committed readback versus complete rollback absence.", "required_change": "Add deterministic committed-but-unacknowledged and rolled-back branches with exact tuple/byte equality, no second admission/envelope regeneration and no partial state.", "status": "ADDRESSED_IN_V7"},
        {"category": "oracle", "issue_class": "finalization_expiry_fence_state_oracle_gap", "criterion_ref": "M4-77-AC-08", "affected_case_ids": ["M4-77-TC-10"], "affected_fields": ["post_lock_database_time", "lease_expiry", "observation", "audit", "terminal_state", "provider_truth"], "problem": "V6 did not state the complete durable-state oracle after expiry for delayed success and every closed failure.", "required_change": "Start with provider-only truth and require fresh post-lock rejection plus an identical before/after PostgreSQL snapshot with no observation, audit or terminal rewrite while provider truth stays readable.", "status": "ADDRESSED_IN_V7"},
        {"category": "evidence", "issue_class": "provider_closed_rejection_atomicity_gap", "criterion_ref": "M4-77-AC-06", "affected_case_ids": ["M4-77-TC-07", "M4-77-TC-08", "M4-77-TC-10"], "affected_fields": ["target_not_found", "validation_rejected", "policy_rejected", "sqlite_ledger", "logical_execution_id", "request_fingerprint", "provider_restart", "EV-04"], "problem": "V6 did not independently bind atomic and restart evidence for all three closed rejection classes.", "required_change": "Parameterize each rejection and bind EV-04 to one exact ledger outcome, zero target effects, exact logical identity/fingerprint, rollback absence, replay and provider restart persistence.", "status": "ADDRESSED_IN_V7"}
      ]
    }
  },
  "packet_digest": "sha256:748458cb2c501781f3538ee2661938d176e59d0c6f9d710ffe533e99d798b05b",
  "request_id": "review-request-sha256:a7e46ff73246f0690b0e6e4ef8e353187798c482f275d7523dc56b140757c333"
}
